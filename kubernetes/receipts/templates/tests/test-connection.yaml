apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "receipts.fullname" . }}-test-connection"
  labels:
    {{- include "receipts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "receipts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
