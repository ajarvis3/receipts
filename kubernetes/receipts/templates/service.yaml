apiVersion: v1
kind: Service
metadata:
  name: {{ include "receipts.fullname" . }}
  labels:
    {{- include "receipts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "receipts.selectorLabels" . | nindent 4 }}
